export default function Terminal() {
  return (
    <div>
      <h1 style={{ marginBottom: "1.5rem" }}>Terminal</h1>
      <p className="prose">
        A real console over the same API &mdash; every command issues an actual
        request. Being built next.
      </p>
    </div>
  );
}
